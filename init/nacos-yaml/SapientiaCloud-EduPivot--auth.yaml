feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full

spring:
  jpa:
    open-in-view: false

oauth2:
  github:
    client-id: ${GITHUB_CLIENT_ID:Ov23litqHal3tJSRdGLD}
    client-secret: ${GITHUB_CLIENT_SECRET:2b5cc1dc3e5495559ce2e49a9e148217f9c21e5e}
    redirect-uri: http://localhost:31600/api/auth/oauth2/callback/github
    scope: user:email,read:user