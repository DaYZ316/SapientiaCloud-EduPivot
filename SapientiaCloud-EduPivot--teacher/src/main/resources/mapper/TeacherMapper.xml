<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dayz.sapientiacloud_edupivot.teacher.mapper.TeacherMapper">

    <resultMap id="Teacher" type="com.dayz.sapientiacloud_edupivot.teacher.entity.po.Teacher">
        <id column="id" property="id"/>
    </resultMap>

    <select id="listTeacher" resultType="com.dayz.sapientiacloud_edupivot.teacher.entity.vo.TeacherVO">
        SELECT
        id, teacher_code, real_name, birth_date, department, education,
        specialization, description, sys_user_id, create_time, update_time
        FROM
        mg_teacher
        <where>
            <if test="teacherCode != null and teacherCode != ''">
                AND teacher_code LIKE CONCAT('%', #{teacherCode}, '%')
            </if>
            <if test="realName != null and realName != ''">
                AND real_name LIKE CONCAT('%', #{realName}, '%')
            </if>
            <if test="department != null and department != ''">
                AND department LIKE CONCAT('%', #{department}, '%')
            </if>
            <if test="education != null">
                AND education = #{education}
            </if>
            <if test="specialization != null and specialization != ''">
                AND specialization LIKE CONCAT('%', #{specialization}, '%')
            </if>
            <if test="startTime != null or endTime != null">
                <choose>
                    <when test="startTime != null and endTime == null">
                        AND create_time &gt;= #{startTime}
                    </when>
                    <when test="startTime == null and endTime != null">
                        AND create_time &lt;= #{endTime}
                    </when>
                    <otherwise>
                        AND create_time BETWEEN #{startTime} AND #{endTime}
                    </otherwise>
                </choose>
            </if>
            AND is_deleted = 0
        </where>
        <if test="orderByColumn != null and orderByColumn != ''">
            ORDER BY ${orderByColumn} ${isAsc == 'asc' ? 'ASC' : 'DESC'}
        </if>
        <if test="orderByColumn == null or orderByColumn == ''">
            ORDER BY create_time DESC
        </if>
    </select>

    <select id="selectByTeacherCode" resultType="com.dayz.sapientiacloud_edupivot.teacher.entity.po.Teacher">
        SELECT *
        FROM mg_teacher
        <where>
            teacher_code = #{teacherCode}
            AND is_deleted = 0
        </where>
    </select>

    <select id="selectByUserId" resultType="com.dayz.sapientiacloud_edupivot.teacher.entity.po.Teacher">
        SELECT *
        FROM mg_teacher
        <where>
            sys_user_id = #{sysUserId}
            AND is_deleted = 0
        </where>
    </select>

    <select id="listAllTeacherWithUserInfo" resultType="com.dayz.sapientiacloud_edupivot.teacher.entity.vo.TeacherVO">
        SELECT t.id,
               t.teacher_code,
               t.real_name,
               t.birth_date,
               t.department,
               t.education,
               t.specialization,
               t.description,
               t.sys_user_id,
               t.create_time,
               t.update_time,
               u.avatar,
               u.username,
               u.nick_name,
               u.email,
               u.mobile,
               u.gender,
               u.status,
               u.last_login_time
        FROM mg_teacher t
                 LEFT JOIN sys_user u ON t.sys_user_id = u.id AND u.is_deleted = 0
        WHERE t.is_deleted = 0
        ORDER BY t.create_time DESC
    </select>

</mapper>